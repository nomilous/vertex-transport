const {basename} = require('path');
const filename = basename(__filename);

xdescribe(filename, () => {

  context('small frames', () => {

    it('compare', done => {

    });

  });

  context('large frames', () => {

    it('compare', done => {

    });

  });

});
